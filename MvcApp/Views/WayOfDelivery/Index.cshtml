@model IEnumerable<MvcApp.Models.WayOfDelivery>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.deliveryText)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.deliveryText)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.wayOfDeliveryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.wayOfDeliveryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.wayOfDeliveryId })
        </td>
    </tr>
}

</table>
