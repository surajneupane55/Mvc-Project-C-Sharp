@model MvcApp.Models.WayOfDelivery

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>WayOfDelivery</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.deliveryText)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.deliveryText)
            @Html.ValidationMessageFor(model => model.deliveryText)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
