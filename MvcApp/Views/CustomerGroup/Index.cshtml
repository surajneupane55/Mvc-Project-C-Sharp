@model IEnumerable<MvcApp.Models.CustomerGroup>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.groupName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.groupDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.groupName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.groupDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.customerGroupId }) |
            @Html.ActionLink("Details", "Details", new { id=item.customerGroupId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.customerGroupId })
        </td>
    </tr>
}

</table>
