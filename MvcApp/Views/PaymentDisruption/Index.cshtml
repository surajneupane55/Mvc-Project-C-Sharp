@model IEnumerable<MvcApp.Models.PaymentDisruption>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.paymentDisruptionType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.paymentDisruptionType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.paymentDisruptionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.paymentDisruptionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.paymentDisruptionId })
        </td>
    </tr>
}

</table>
