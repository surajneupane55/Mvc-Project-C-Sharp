@model MvcApp.Models.PaymentDisruption

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>PaymentDisruption</legend>

        @Html.HiddenFor(model => model.paymentDisruptionId)

        <div class="editor-label">
            @Html.LabelFor(model => model.paymentDisruptionType)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.paymentDisruptionType)
            @Html.ValidationMessageFor(model => model.paymentDisruptionType)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
